<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="VolunteerListWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">


    <!--External Grid-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--Internal, Upper Grid-->
        <Grid Height="auto" Grid.Row="0" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="select volunteer" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Height="32" />
            <ComboBox   ItemsSource="{Binding Source={StaticResource VolunteerCollectionKey}}" SelectedValue="{Binding VolunteerSort, Mode=TwoWay}" Grid.Column="1"            
                        SelectionChanged="ComboBox_SelectionChanged"
 Height="32" VerticalAlignment="Top"/>
            <TextBox Grid.Column="2"
                     Margin="5"
                     Height="32"
                     VerticalAlignment="Top"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     /> 
        </Grid>



        <ListView ItemsSource="{Binding VolunteerList}" HorizontalAlignment="Left" Height="336" Margin="40,0,0,0" Grid.Row="1"  VerticalAlignment="Center" Width="552" SelectedItem="{Binding SelectedVolunteer, Mode=TwoWay}" MouseDoubleClick="Volunteer_MouseDoubleClick">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="VolunteerId" DisplayMemberBinding="{Binding VolunteerId}" Width="Auto"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding FullName}" Width="Auto"/>
                    <GridViewColumn Header="Handled Requests" DisplayMemberBinding="{Binding TotalHandledRequests}" Width="Auto"/>
                    <GridViewColumn Header=" Canceled Requests" DisplayMemberBinding="{Binding TotalCanceledRequests}" Width="Auto"/>


                    <GridViewColumn Header="Is Active">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" IsEnabled="False" Width="Auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn  Width="Auto">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Click="DeleteVolunteer_click"   CommandParameter="{Binding}"  />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                </GridView>
            </ListView.View>
        </ListView>




        <Button Content="Add" Grid.Row="1" HorizontalAlignment="Center" Height="50" Width="100" Margin="0,356,0,0" Grid.RowSpan="2" VerticalAlignment="Top" Click="Add_Click"/>

    </Grid>

</Window>
<!--Command="{Binding ButtonText, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding ButtonText}"--> 
