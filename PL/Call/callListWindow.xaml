<Window x:Class="PL.Call.CallListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Call List" Height="640" Width="900"
        Background="#F9FAFC"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <!--External Grid-->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Internal, Upper Grid-->
        <Grid HorizontalAlignment="Stretch" Height="Auto" Grid.Row="0" VerticalAlignment="Stretch" Width="Auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Select Semester:" Grid.Column="0" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource CallFieldCollectionKey}}"
                      SelectedValue="{Binding CallFilter, Mode=TwoWay}"
                      SelectionChanged="ComboBox_SelectionChanged"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="1"/>
            <TextBox Margin="28,0,37,0" VerticalAlignment="Center"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Grid.Column="2" Height="39"/>
        </Grid>

        <!-- ListView -->
        <ListView ItemsSource="{Binding CallList}"
                  SelectedItem="{Binding SelectedCall, Mode=TwoWay}"
                  MouseDoubleClick="Call_MouseDoubleClick"
                  Grid.Row="1" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Assignment ID" DisplayMemberBinding="{Binding AssignmentId}" Width="80"/>
                    <GridViewColumn Header="Call ID" DisplayMemberBinding="{Binding CallId}" Width="80"/>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding TypeOfReading}" Width="100"/>
                    <GridViewColumn Header="Opening Time" DisplayMemberBinding="{Binding OpeningTime}" Width="120"/>
                    <GridViewColumn Header="Remaining Time" DisplayMemberBinding="{Binding RemainingTimeToEndCall}" Width="120"/>
                    <GridViewColumn Header="Last Volunteer" DisplayMemberBinding="{Binding LastVolunteerName}" Width="150"/>
                    <GridViewColumn Header="Total Handling Time" DisplayMemberBinding="{Binding TotalHandlingTime}" Width="120"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="100"/>
                    <GridViewColumn Header="Total Assignments" DisplayMemberBinding="{Binding TotalAssignments}" Width="120"/>
                    <GridViewColumn Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Click="DeleteCall_click"
                                        CommandParameter="{Binding}" Background="#F87171"
                                        Foreground="White" Padding="4"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- כפתור ה-Add -->
        <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Height="50" Width="100"/>
    </Grid>
</Window>
