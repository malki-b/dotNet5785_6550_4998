<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Volunteer Details" Height="700" Width="700" Background="#F9FAFC"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid Margin="30">
        <Grid.Resources>
            <Style TargetType="Label">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="4"/>
            </Style>
            <Style x:Key="InputStyle" TargetType="Control">
                <Setter Property="Margin" Value="4"/>
                <Setter Property="MinWidth" Value="220"/>
                <Setter Property="Padding" Value="4"/>
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- שדות -->
        <Label Content="ID:" Grid.Row="0" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.Id}" Style="{StaticResource InputStyle}" Grid.Row="0" Grid.Column="1" IsReadOnly="True"/>

        <Label Content="Name:" Grid.Row="1" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.FullName}" Style="{StaticResource InputStyle}" Grid.Row="1" Grid.Column="1"/>

        <Label Content="Phone:" Grid.Row="2" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.Phone}" Style="{StaticResource InputStyle}" Grid.Row="2" Grid.Column="1"/>

        <Label Content="Email:" Grid.Row="3" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.Email}" Style="{StaticResource InputStyle}" Grid.Row="3" Grid.Column="1"/>

        <Label Content="Password:" Grid.Row="4" Grid.Column="0"/>
        <PasswordBox Grid.Row="4" Grid.Column="1" Style="{StaticResource InputStyle}"/>

        <Label Content="Role:" Grid.Row="5" Grid.Column="0"/>
        <ComboBox ItemsSource="{Binding Source={StaticResource RolesCollectionKey}}" 
                  SelectedItem="{Binding CurrentVolunteer.Role}" 
                  Style="{StaticResource InputStyle}" Grid.Row="5" Grid.Column="1"/>

        <Label Content="Active?" Grid.Row="6" Grid.Column="0"/>
        <CheckBox IsChecked="{Binding CurrentVolunteer.IsActive}" 
                  Style="{StaticResource InputStyle}" Grid.Row="6" Grid.Column="1"/>

        <Label Content="Distance Type:" Grid.Row="7" Grid.Column="0"/>
        <ComboBox ItemsSource="{Binding Source={StaticResource DistanceCollectionKey}}" 
                  SelectedItem="{Binding CurrentVolunteer.TypeDistance}" 
                  Style="{StaticResource InputStyle}" Grid.Row="7" Grid.Column="1"/>

        <Label Content="Address:" Grid.Row="8" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.Address}" 
                 Style="{StaticResource InputStyle}" Grid.Row="8" Grid.Column="1"/>

        <Label Content="Max Distance:" Grid.Row="9" Grid.Column="0"/>
        <TextBox Text="{Binding CurrentVolunteer.MaxDistance}" 
                 Style="{StaticResource InputStyle}" Grid.Row="9" Grid.Column="1"/>

        <!-- כפתור עם Border -->
        <!--<Border CornerRadius="6" Background="#4CD8C2" Margin="98,35,199,-74" Grid.Column="1" Grid.Row="11">
            <Button Content="{Binding ButtonText}" 
                    Click="AddAndUpdate_Click" Width="223" 
                    HorizontalAlignment="Center" 
                    Foreground="#4CD8C2" FontWeight="Bold"
                    BorderThickness="0"/>
        </Border>-->
        <Border CornerRadius="6" Background="#4CD8C2" Margin="98,35,199,-74" Grid.Column="1" Grid.Row="11">
            <Button Content="{Binding ButtonText}" 
            Click="AddAndUpdate_Click" Width="224" 
            HorizontalAlignment="Center" 
            Foreground="White" FontWeight="Bold"
            Background="#4CD8C2" BorderThickness="0"/>
        </Border>


    </Grid>
</Window>
